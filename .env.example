# Firebase Configuration
# Firebase service account key path (optional - if not provided, will use Firebase client SDK only)
# FIREBASE_SERVICE_ACCOUNT_KEY=/path/to/your/service-account-key.json

# API Configuration
API_PORT=8000
API_DEBUG=False

# Database Configuration (for Docker deployment)
DATABASE_URL=postgresql://hue:huepassword@db:5432/hue_db
REDIS_URL=redis://redis:6379

# Environment
ENVIRONMENT=development